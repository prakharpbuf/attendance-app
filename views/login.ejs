<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/favicon.png" />
    <title>AS333 Attendance</title>
    <link rel="stylesheet" href="login.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
</head>

<body>
    <div id="container">
        <img src="./bg.jpg" alt="Background image" id="desktop-img">
        <img src="./bg-mb.jpg" alt="Background image for mobile" id="mobile-img">
        <div>
            <form action="auth" method="post">
                <label for="email">Email:</label>
                <input type="text" name="email">
                <label for="password">Password:</label>
                <input type="password" name="password">
                <br>
                <% if (messages.error) { %>
                <p style="color:red; font-size: 0.5em;"><%- messages.error %></p>
                <% } %>
                <% if (messages.success) { %>
                <p style="color:green; font-size: 0.5em;"><%- messages.success %></p>
                <% } %>
                <button type="submit">Login</button>
            </form>
        </div>
    </div>
</body>

</html>